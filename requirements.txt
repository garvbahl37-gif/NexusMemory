# Serverless dependency set — this is what Vercel installs.
#
# No torch, no sentence-transformers, no chromadb: together they are roughly
# 1 GB against a 250 MB function limit. Embeddings are served over HTTP
# (backend/services/embeddings_provider.py) and vectors live in pgvector,
# queried directly rather than through langchain-community.
fastapi==0.141.1
python-multipart==0.0.32
langchain-core==1.6.2
langchain-text-splitters==1.1.2
langchain-groq==1.1.3
langchain-openai==1.6.0
pypdf==6.17.0
python-docx==1.2.0
sqlalchemy==2.0.52
psycopg2-binary==2.9.12
pgvector==0.5.0
pydantic==2.13.5
pydantic-settings==2.15.0
python-dotenv==1.2.3
aiofiles==25.1.0
httpx==0.28.1
redis==8.1.0
